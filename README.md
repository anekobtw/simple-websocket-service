#websocket

That's a simple text syncing system implemented via WebSockets.

There are 2 frontend apps:
- Sender (writes text)
- Receiver (displays text)

Both connect to:
`ws://localhost:8080/text`



